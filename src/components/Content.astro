<div class="content">
  <slot />
</div>

<style>
  .content {
    flex: 1;
    align-self: center;
    max-width: 540px;
    font-size: 0.9em;
    padding: 20px var(--safe-area-padding);
  }

  @media (min-width: 720px) {
    .content {
      padding: 80px var(--safe-area-padding);
    }
  }

  :global(.content a) {
    color: var(--link-color);
    text-decoration: underline;
    text-decoration-style: wavy;
  }

  :global(.content h1),
  :global(.content h2) {
    word-spacing: 0.075em;
    font-family: "Corben", serif;
    font-weight: 400;
  }
  :global(.content ul),
  :global(.content ol) {
    margin-left: 1.5rem;
  }

  :global(.content ul) {
    list-style-type: square;
  }

  :global(.content > * + *) {
    margin-top: 1rem;
  }

  :global(.content img),
  :global(.content video) {
    max-width: 100%;
    height: auto;
    border-radius: 12px;
    margin: 0 auto;
  }

  :global(.content hr) {
    margin: 2rem 0;
    border: 0;
    border-top: 1px dashed var(--body-secondary-foreground-color);
  }

  :global(.content code) {
    color: var(--color-accent);
    background-color: color-mix(in srgb, var(--color-accent) 15%, transparent);
    padding: 0.2em 0.4em;
    border-radius: 4px;
    font-size: 0.9em;
    font-weight: 500;
    font-family: "SF Mono", SFMono-Regular, ui-monospace, "DejaVu Sans Mono",
      Menlo, Consolas, monospace;
  }

  :global(.content table) {
    width: 100%;
    border-collapse: separate;
    border-spacing: 0;
    border-radius: 12px;
    border: 1px solid
      color-mix(in srgb, var(--body-foreground-color) 10%, transparent);
    overflow: hidden;
    margin: 1rem 0;
  }

  :global(.content th) {
    background-color: color-mix(
      in srgb,
      var(--body-foreground-color) 10%,
      transparent
    );
    text-align: left;
    padding: 0.75rem 1rem;
    font-weight: 500;
  }

  :global(.content td) {
    padding: 0.75rem 1rem;
  }

  :global(.content tr:nth-child(even)) {
    background-color: color-mix(
      in srgb,
      var(--body-foreground-color) 5%,
      transparent
    );
  }

  :global(.content tr:nth-child(odd)) {
    background-color: transparent;
  }

  :global(.content tr:last-child td:first-child) {
    border-bottom-left-radius: 11px;
  }

  :global(.content tr:last-child td:last-child) {
    border-bottom-right-radius: 11px;
  }

  :global(.content th:first-child) {
    border-top-left-radius: 11px;
  }

  :global(.content th:last-child) {
    border-top-right-radius: 11px;
  }
</style>
